#include <stdio.h>

/**
 * int_add - a function that adds two integers,
 * and returns the result
 *
 * Return: Always return 0 (Success).
 */

int add(int a, int b)
{
	int res;

	res = a + b;
	return(res);
}
