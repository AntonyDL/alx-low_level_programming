#include <stdio.h>

/**
 * add - a function that adds two integers,
 * and returns the result
 * @a: int added to b
 * @b: int added to a
 * res: sum of a and b
 *
 * Return: res - sum of a+b.
 */

int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
