#include "main.h"

/**
 * print_to_98 - function that prints all natural number
 * form n to 98, followed by a new line
 *
 * Return: Always a (0) as (Success)
 */

void print_to_98(int n)
{
	if (n=0 && n<98, i++)
	{
		_putchar("%n\n", n);
		i++;

		_putchar(',');
		_puthcar(' ');
		_putchar('a-z', '0-98');
		_putchar(first_number=0);
		_putchar(last_number=98);

		_putchar("\n");
		return (0);
	}
}
