#incluse <stdio.h>

/*
*main-entry point of the program
*
*Return: always returns 0 when code (Success)
*/
int main(void)
{
	char letter = 'a';
	while (letter <= 'z';)
	{
		putchar(letter);
		letter++;
	}
	putchar('\n');
	return (0);
}
